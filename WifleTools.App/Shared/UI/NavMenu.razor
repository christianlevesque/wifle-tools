@using System
@inject State.LayoutState LayoutState

<MudDrawer Elevation="0"
           Variant="DrawerVariant.Persistent"
           Open>
	<MudDrawerHeader Class="flex-column">
		<MudText Typo="Typo.h4">
			Wifle Tools
		</MudText>

		<MudText Typo="Typo.h6">
			@LayoutState.Subtext
		</MudText>
	</MudDrawerHeader>
	<MudNavMenu>
		@if (!string.IsNullOrEmpty(LayoutState.BackUrl))
        {
            <MudNavLink Href="@LayoutState.BackUrl"
                        Icon="@Icons.Material.Filled.ArrowBack"
                        IconColor="Color.Inherit"
                        Match="NavLinkMatch.All">
            	Go back
            </MudNavLink>
        }

        @foreach (var navItem in LayoutState)
        {
            <MudNavLink Href="@navItem.Url"
                        Icon="@navItem.Icon"
                        IconColor="Color.Inherit"
                        Match="NavLinkMatch.All">
            	@navItem.Text
            </MudNavLink>
        }

		<MudDivider Class="my-3"/>

		<MudNavLink Href="@Urls.About"
		            Icon="@Icons.Material.Outlined.Info"
		            IconColor="Color.Inherit"
		            Match="NavLinkMatch.All">
			About
		</MudNavLink>
	</MudNavMenu>
</MudDrawer>
